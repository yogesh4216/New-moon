{
  "// note": "Node-side tooling (deploy, setup, wallet, CLI, diagnostics). Run through tsx, which strips types rather than checking them, so this config is what checks them. Globbed on purpose: a hand-listed set silently skipped show-address.ts and diagnose-sync.ts, and a stale call there shipped.",
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "strict": false,
    "noEmit": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "allowSyntheticDefaultImports": true,
    "types": [
      "node"
    ],
    "lib": [
      "ES2022"
    ]
  },
  "include": [
    "src/*.ts"
  ],
  "exclude": [
    "src/config.ts",
    "src/vite-env.d.ts"
  ]
}
